export default class Item {
    constructor(id, name, madeOf, amount, completed) {
        this.id = id;
        this.name = name;
        this.madeOf = madeOf;
        this.amount = amount;
        this.completed = completed;
    }
}